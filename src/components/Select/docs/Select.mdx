import { Meta, Canvas, Story, ArgTypes } from '@storybook/blocks';
import Select from '../Select.vue'
import * as SelectStories from './Select.stories';
import { Title, Controls } from '@storybook/addon-docs'

<Meta of={SelectStories} />

<Title />

> 下拉选择器。<br /><br />
> 弹出一个下拉菜单给用户选择操作，用于代替原生的选择器，或者需要一个更优雅的多选器时。<br /><br />
> 当选项少时（少于 5 项），建议直接将选项平铺，使用 Radio 是更好的选择。

```ts
import { Select } from "cms-antd-v";
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.Default.storyName}

> 最简单的用法。

<Canvas of={SelectStories.Default} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.TagsComponent.storyName}

> tags select，随意输入的内容（scroll the menu）

<Canvas of={SelectStories.TagsComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.AutoSliptComponent.storyName}

> 试下复制 露西,杰克 到输入框里。只在 tags 和 multiple 模式下可用。

<Canvas of={SelectStories.AutoSliptComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.MultipleComponent.storyName}

> 多选 multiple，多选，从已有条目中选择（scroll the menu）

<Canvas of={SelectStories.MultipleComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.GroupComponent.storyName}

> 用 `OptGroup` 或 `options.options` 进行选项分组。

<Canvas of={SelectStories.GroupComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.SearchComponent.storyName}

> 展开后可对选项进行搜索。

<Canvas of={SelectStories.SearchComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.SuffixIconComponent.storyName}

> 自定义后缀图标。

<Canvas of={SelectStories.SuffixIconComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.DropdownRenderComponent.storyName}

> 使用 `dropdownRender` 对下拉菜单进行自由扩展。

<Canvas of={SelectStories.DropdownRenderComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.BigCountComponent.storyName}

> Select 使用了虚拟滚动技术，因而获得了比 1.x 更好的性能

<Canvas of={SelectStories.BigCountComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SelectStories.PlacementComponent.storyName}

> 可以通过 `placement` 手动指定弹出的位置。

<Canvas of={SelectStories.PlacementComponent} />

---

## API

### 属性

### Select

<ArgTypes of={Select} />
