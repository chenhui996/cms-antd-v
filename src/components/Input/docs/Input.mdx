import { Meta, Canvas, Story, ArgTypes } from '@storybook/blocks';
import Input from '../Input.vue'
import * as InputStories from './Input.stories';
import { Title, Controls } from '@storybook/addon-docs'

<Meta of={InputStories} />

<Title />

> 通过鼠标或键盘输入内容，是最基础的表单域的包装。<br /><br />
> 使用：需要用户输入表单域内容时。提供组合型输入框，带搜索的输入框，还可以进行大小选择。

```ts
import { Input } from "cms-antd-v";
```
{/* 
{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.Default.storyName}

> 最简单的用法。

<Canvas of={InputStories.Default} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.PrefixAndSuffixComponent.storyName}

> 在输入框上添加前缀或后缀图标。。

<Canvas of={InputStories.PrefixAndSuffixComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.AutoHeightTextAreaComponent.storyName}

> 属性适用于 textarea 节点，并且只有高度会自动变化。另外 autoSize 可以设定为一个对象，指定最小行数和最大行数
>> 1.5.0 后 autosize 被废弃，请使用 autoSize。

<Canvas of={InputStories.AutoHeightTextAreaComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.SearchComponent.storyName}

> 带有搜索按钮的输入框。

<Canvas of={InputStories.SearchComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.SearchLoadingComponent.storyName}

> 用于 `onSearch` 的时候展示 loading。

<Canvas of={InputStories.SearchLoadingComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.SizeComponent.storyName}

> 我们为 `<Input />` 输入框定义了三种尺寸（大、默认、小），高度分别为 `40px`、`32px` 和 `24px`。

<Canvas of={InputStories.SizeComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.DifferentInputComponent.storyName}

> 输入框的组合展现。 注意：使用 `compact` 模式时，不需要通过 `Col` 来控制宽度。

<Canvas of={InputStories.DifferentInputComponent} />

> 依赖引用：

```ts
import { CopyOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.FormatTooltipComponent.storyName}

> 结合 `Tooltip` 组件，实现一个数值输入框，方便内容超长时的全量展现。

<Canvas of={InputStories.FormatTooltipComponent} />

> 依赖引用：

```ts
import { CheckOutlined, CloseOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.AddonComponent.storyName}

> 设置 `addonBefore` 和 `addonAfter` 属性，可以添加前置和后置标签。

<Canvas of={InputStories.AddonComponent} />

> 依赖引用：

```ts
import { SettingOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.ClearIconComponent.storyName}

> 带移除图标的输入框，点击图标删除所有内容。

<Canvas of={InputStories.ClearIconComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.PasswordComponent.storyName}

> 密码框。

<Canvas of={InputStories.PasswordComponent} />

> 依赖引用：

```ts
import { EyeTwoTone, EyeInvisibleOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.TipNumberComponent.storyName}

> 带计数器的输入框。

<Canvas of={InputStories.TipNumberComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.TextareaComponent.storyName}

> 文本域 textarea。

<Canvas of={InputStories.TextareaComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.StatusComponent.storyName}

> 使用 `status` 为 Input 添加状态，可选 `error` 或者 `warning`。

<Canvas of={InputStories.StatusComponent} />

> 依赖引用：

```ts
import { ClockCircleOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {InputStories.NoBorderComponent.storyName}

> 没有边框。

<Canvas of={InputStories.NoBorderComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}


---

## API

### 属性

### Input

<ArgTypes of={Input} />
