import { Meta, Canvas, Story, ArgTypes } from '@storybook/blocks';
import { reactive, toRefs } from 'vue';
import Switch from '../Switch.vue'
import * as SwitchStories from './Switch.stories';
import { Title, Controls } from '@storybook/addon-docs'

<Meta of={SwitchStories} />

<Title />

> 需要表示开关状态/两种状态之间的切换时；<br /><br />
> 和 `checkbox` 的区别是，切换 `switch` 会直接触发状态改变，而 `checkbox` 一般用于状态标记，需要和提交操作配合。

```ts
import { Switch } from "cms-antd-v";
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SwitchStories.Default.storyName}

> 最简单的用法。

<Canvas of={SwitchStories.Default} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SwitchStories.SizeComponent.storyName}

> `size="small"` 表示小号开关。

<Canvas of={SwitchStories.SizeComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SwitchStories.DisabledComponent.storyName}

> 添加 `disabled` 属性即可让开关处于不可用状态，同时开关样式也会改变。

<Canvas of={SwitchStories.DisabledComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SwitchStories.SlotComponent.storyName}

> 带有文字和图标。

<Canvas of={SwitchStories.SlotComponent} />

> 依赖引用：

```ts
import { CheckOutlined, CloseOutlined } from '@ant-design/icons-vue';
```

{/* ------------------------------------------------------------------------------------------------------------------------ */}

## {SwitchStories.LoadingComponent.storyName}

> 标识开关操作仍在执行中。

<Canvas of={SwitchStories.LoadingComponent} />

{/* ------------------------------------------------------------------------------------------------------------------------ */}

---

## API

### 属性

### Button

<ArgTypes of={Switch} />
