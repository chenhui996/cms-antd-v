import {Meta, Canvas,Story, ArgTypes} from '@storybook/blocks';
import { reactive, toRefs } from 'vue';
import Button from './Button.vue'
import * as ButtonStories from './Button.stories';
import { Title } from '@storybook/addon-docs'

<Meta of={ButtonStories} />

<Title />

按钮用于开始一个即时操作。

```js
import { Button } from "cms-antd-v";
```

## 按钮类型
<Canvas of={ButtonStories.Default}></Canvas>

## 不可用状态
<Canvas of={ButtonStories.不可用状态}></Canvas>

## 加载中状态
<Canvas of={ButtonStories.加载中状态}></Canvas>

## 图标按钮
<Canvas of={ButtonStories.图标按钮}></Canvas>

## 按钮尺寸
<Canvas of={ButtonStories.按钮尺寸}></Canvas>

## 危险按钮
<Canvas of={ButtonStories.危险按钮}></Canvas>

## API
<ArgTypes of={Button} />
